RULESET FOR VERSION 0.XX  OF NN_FS:

Programming Language:
- all code to be written in C++

Packages and Libraries:
- only libraries from the standard c++ library can be used on this project
	-> obviously no packages from tensor flow or torch or anything else ML/AI related
- if there are any standard c++ libraries that are related to ML or AI ... these cannot be used

Algorithm Requirements?: // probably don't need to do this as it will already be hard enough as it is

#include <iostream>
#include <vector>

// program to run the backpropogation and adjust the weights
// this program shouldn't have a main () function ... just a class of functions or list of functions

//    BACKPROPOGATION ALGORITHM CPP FILE




// RELU function to be applied after the raw sum function
//  -> take in the raw sum of a neuron 
double RELu(double raw_sum){

}


// activation being calculated for one neuron
//  -> take the previous layers previous layers activations 
//  -> take the updated weights between the previous and current layers
//  -> take the biases for the current layer
double raw_sum(vector<double>* previous_activationsPtr, double* biasPtr, std::vector<std::vector<double>>* WeightsPtr, int current_neuron_number){

  return ;
}
